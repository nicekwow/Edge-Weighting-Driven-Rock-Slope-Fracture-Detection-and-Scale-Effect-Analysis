# 三幅重建图像与参考标注

Image a、b、c 分别对应 `a11`、`a13`、`a14`。每组包含原图 PNG、LabelMe JSON 和二值掩码 `*_mask.png`；掩码中 0 为背景，255 为裂隙。JSON 中 `fracture` 表示裂隙，`background` 多边形表示裂隙内部的孔洞。

这些参考标注依据早期图 13 的显示面板恢复，再映射到 2560 × 1440 像素原图。对面积或跨度很小的孤立碎片进行了筛除。由于显示面板经过缩小和栅格化，本目录的掩码不能视为已不在档案中的原始全分辨率标注。使用这些掩码评价预测时，应重新计算指标。
